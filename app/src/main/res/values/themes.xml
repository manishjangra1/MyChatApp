<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyChatApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyChatApp" parent="Base.Theme.MyChatApp">
        <!-- Customize your dark theme here. -->
         <item name="colorPrimary">@color/color4</item>
        <item name="android:statusBarColor" tools:targetApi="31">@color/color4</item>
        <item name="android:colorBackground">@color/extra_light_color22</item>
    </style>
</resources>